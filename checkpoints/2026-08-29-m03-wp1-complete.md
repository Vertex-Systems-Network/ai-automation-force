# M03 WP1 Complete — Storage Adapter and Object Metadata

Date: 2026-08-29

Status: **M03_WP1_COMPLETE_WP2_ACTIVE**

## Authorization

M03 remains authorized by the fresh milestone-specific operator approval:

`Milestone 3 development approve — start.`

This checkpoint does not extend authorization beyond M03.

## Accepted work package

**M03-WP1 — Storage adapter and object metadata** / Linear `ABD-200`

Canonical merge:
`b2cdad89f514ca905c66c71f44835ca8fbac9a51`

Accepted replacement PR:
`#27 — M03-WP1: storage adapter and object metadata`

Accepted executable head:
`9486b6108728264c878f467c63ac56f1f7414ecd`

Draft PR #26 carried the same code head but was closed unmerged because the connected GitHub draft→ready mutation returned an upstream GraphQL response-schema error. PR #27 was opened non-draft from the exact same head and received fresh CI before merge.

## Fresh acceptance evidence

Replacement PR #27 exact-head checks:

- Core Domain Contracts #135 — run `33263036122` / job `99128136646` — GREEN
- Durable Control Plane #73 — run `33263036135` / job `99128136832` — GREEN

Verified gates include:

- Ruff;
- strict mypy;
- unit tests;
- PostgreSQL integration and reversible migration chain;
- Temporal/M02 durable-control regression suite;
- generated core schema synchronization;
- deterministic OpenAPI generation;
- package/API/worker/migration compile checks.

## Accepted WP1 capability

- provider-neutral physical `StorageObject` contract, separate from canonical business `Asset` identity;
- stable `STO-*` identity and deterministic opaque object-key construction;
- key/path validation with traversal/root-escape protection;
- deterministic filesystem adapter with contained paths and atomic immutable writes;
- S3-compatible boto3/botocore `1.43.83` adapter with configurable endpoint/region/addressing;
- conditional S3 creation using `If-None-Match: *` plus concurrent-writer reconciliation;
- SHA-256 derived from actual bytes; ETag retained only as storage-backend metadata;
- MIME, byte size, backend, region, version ID, lifecycle class and audit metadata;
- persisted schema-version validation fails closed for unsupported versions;
- additive/reversible `core.storage_objects` migration `20260829_0008`;
- PostgreSQL idempotency and physical-location uniqueness;
- deterministic generated `storage-object.schema.json`;
- matching dev-only `types-boto3[s3]==1.43.83` for strict typing.

## Scope proof

WP1 did not implement:

- upload sessions or multipart control (WP2);
- quarantine/media probing/security pipeline (WP3);
- Asset↔StorageObject provenance/rights linkage (WP4);
- derivatives/proxies (WP5);
- signed delivery (WP6);
- retention/archive/delete/export orchestration (WP7);
- real provider/cloud spend or credentials;
- M04+ functionality.

The low-level storage adapter delete primitive is not lifecycle policy; lifecycle orchestration remains WP7.

## Linear mirror

- `ABD-200` — Done after merge + fresh required CI evidence.
- `ABD-201` — next executable M03 target.
- `ABD-202` through `ABD-207` remain blocked/sequenced behind their planned predecessors.

## Next executable target

**M03-WP2 — Upload sessions and resumable multipart control** / Linear `ABD-201`

WP2 may implement signed/direct upload-session contracts, multipart/resume state, exact key/size/type binding, quota-reservation hooks, expiry, complete/abort and idempotency.

WP2 must not implement quarantine/probe acceptance (WP3) or later M03 work packages.

GitHub remains canonical. Linear mirrors verified execution state.
