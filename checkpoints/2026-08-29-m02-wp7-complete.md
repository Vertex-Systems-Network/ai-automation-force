# M02-WP7 Complete — Job Control API and Durable SSE Progress

Date: 2026-08-29

Status: **M02_WP7_COMPLETE_WP8_ACTIVE**

## Authorization

M02 executable development remains authorized by the explicit operator consent recorded in:

`checkpoints/2026-08-29-m02-development-approved-governance-reconciled.md`

## Verified executable candidate

WP7 implementation branch:
`m02/wp7-job-control-sse`

Verified candidate head:
`81489ce89290dc05e1b14784d174247ac31701ba`

Merged via non-draft replacement PR `#19`.

Merge commit:
`b1e99d0da6b38ae3c826c15bb594c40b8c34d607`

Draft PR `#18` was closed without code change only because the connected ready-for-review mutation failed at the connector GraphQL schema layer. PR `#19` used the identical branch and candidate head.

## Required CI evidence

Fresh PR #19 exact-head validation:

- Core Domain Contracts run `33258197047` / job `99115470093` — **GREEN**
- Durable Control Plane run `33258197044` / job `99115470409` — **GREEN**

The Durable gate passed Ruff, mypy, PostgreSQL + Temporal integration tests, generated schema synchronization, deterministic OpenAPI generation, and compile/migration checks.

## Delivered WP7 contract

- provider-neutral job/control REST API;
- create/reuse, inspect, start, cancel and retry commands;
- workflow and project control status;
- durable command idempotency ledger in migration `0007`;
- optimistic revision enforcement and retry-budget accounting;
- deterministic workflow IDs and persisted Temporal execution references;
- crash-recovery-aware start reconciliation;
- deterministic cursor-paginated project jobs and job history;
- durable SSE progress replay using existing transactional `core.outbox_messages`;
- stable event cursors and `Last-Event-ID` reconnect semantics;
- snapshot high-water cursor for snapshot-to-stream handoff;
- normalized fail-closed API errors;
- deterministic OpenAPI contract;
- reversible migration `0007 -> 0006`.

No duplicate progress-event persistence layer was introduced; the transactional outbox remains the durable event evidence source.

## Safety and scope boundary

WP7 introduced no:

- real provider endpoint or credential;
- provider spend/free-credit consumption;
- media generation or object-storage implementation;
- WebSocket surface;
- full web/mobile/auth/billing/social implementation;
- M03+ executable work.

## Next executable target

**M02-WP8 — Replay, restart and synthetic 100-shot recovery acceptance**

WP8 must prove the milestone under synthetic/spend-free failure injection: fan-out/join, worker/process restart recovery, API restart independence, retries, duplicate events/callbacks, cancellation, approval wait/resume, retained-history replay and history-growth/Continue-As-New policy where applicable.

M02 is not complete until WP8 passes and the M02 closure checkpoint is recorded.
