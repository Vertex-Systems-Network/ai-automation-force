# M03 WP2 Complete — Upload Sessions and Resumable Multipart Control

Date: 2026-08-29

Status: **M03_WP2_COMPLETE_WP3_ACTIVE**

## Authorization

M03 remains authorized by the fresh milestone-specific operator approval:

`Milestone 3 development approve — start.`

This checkpoint does not extend authorization beyond M03.

## Accepted work package

**M03-WP2 — Upload sessions and resumable multipart control** / Linear `ABD-201`

Canonical merge:
`ff7005b127f0c83df481ae04b85fd763d207a67f`

Accepted replacement PR:
`#30 — M03-WP2: upload sessions and resumable multipart control`

Accepted executable head:
`6c63ca083ac48b0b1127610f66fb0bce60223f22`

Draft PR #29 carried the same branch/head and was closed unmerged because the connected GitHub draft→ready mutation returned an upstream GraphQL response-schema error (`Repository.fullDatabaseId`). PR #30 was opened non-draft from the exact same head and received fresh PR-triggered CI before merge.

## Fresh acceptance evidence

Replacement PR #30 exact-head checks:

- Core Domain Contracts #166 — run `33266539978` / job `99137479531` — GREEN
- Durable Control Plane #104 — run `33266539983` / job `99137479679` — GREEN

Verified gates include:

- Ruff;
- strict mypy;
- unit tests;
- PostgreSQL integration and reversible migration chain;
- Temporal/M02 durable-control regression suite;
- generated core schema synchronization;
- deterministic OpenAPI generation;
- package/API/worker/migration compile checks.

## Accepted WP2 capability

- stable `UPS-*` upload-session identity;
- provider-neutral single and multipart upload-session contracts;
- upload object key derived only as `uploads/quarantine/{project}/{storage-object}`;
- caller-selected arbitrary bucket/object paths rejected;
- exact expected byte size, MIME type and storage-object binding;
- quota-reservation reference hook without commercial billing implementation;
- additive/reversible migration `20260829_0009` for sessions, multipart part ledger and terminal command idempotency;
- row-locked PostgreSQL create/bind/part/complete/abort/expiry transitions;
- durable multipart backend `UploadId` binding with same-ID replay and conflicting-ID rejection;
- create-idempotency remains stable after mutable backend `UploadId` binding;
- semantic part replay compares part number/size/ETag/checksum rather than local observation timestamp;
- direct S3 upload grants bind exact key/content-type/exact size and bounded expiry;
- direct and multipart initiation refuse destructive overwrite of an existing destination object;
- interrupted multipart initialization can recover one existing exact-key multipart `UploadId`; multiple candidates fail closed;
- multipart part grants bind exact bucket/key/UploadId/part number;
- lost completion acknowledgement reconciles `NoSuchUpload` against the final exact-key object and verifies final size;
- repeated complete/abort commands are idempotent while changed semantics conflict;
- expiry/abort are terminal for WP2 transfer state;
- completion means transfer completion only and does not implicitly promote an `Asset`;
- deterministic generated `upload-session.schema.json` and public core exports.

## Scope proof

WP2 did not implement:

- quarantine/media probing/malware or threat acceptance (WP3);
- Asset↔StorageObject provenance/rights linkage (WP4);
- derivatives/proxies (WP5);
- signed media delivery (WP6);
- retention/archive/delete/export orchestration (WP7);
- real provider/cloud credentials or spend;
- M04+ functionality.

## Linear mirror

- `ABD-200` — Done.
- `ABD-201` — Done after canonical merge + fresh required CI evidence.
- `ABD-202` — next executable M03 target.
- `ABD-203` through `ABD-207` remain sequenced behind their planned predecessors.

## Next executable target

**M03-WP3 — Quarantine, media probe and upload security** / Linear `ABD-202`

WP3 may implement quarantine lifecycle, claimed MIME versus actual magic/type verification, size/type policy, resource-limited media probe hooks, malware/threat-scanning hooks and accepted/rejected transfer-security state.

WP3 acceptance must prove malformed/malicious fixtures fail closed, rejected objects never become routable canonical assets, parser/FFmpeg work executes outside deterministic workflow code, and external process invocation is bounded and non-shell-injectable.

WP3 must not implement WP4 provenance/rights linkage or later M03 work packages.

GitHub remains canonical. Linear mirrors verified execution state.
